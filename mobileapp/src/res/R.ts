import colors from 'res/colors'
import linking from 'res/linking'
import notifications from 'res/notifications'
import routes from 'res/routes'
import strings from 'res/strings'

/**
 * Namespace for all project's resources.
 */
const R = {
  strings,
  routes,
  linking,
  colors,
  notifications,
}

export default R
